<h3>{{categoria.nome}}</h3>
{{#each postagens}}
  <div class="card mt-2 bg-dark text-white p-5">
    <div class="card-body">
      <h1>Título: {{titulo}}</h1>
      <p>Descrição: {{descricao}}</p>
      <a href="/postagem/{{slug}}"><button class="btn btn-success">Exibir postagem</button></a>
      <hr>
      <small>Data de criação: {{data}}</small>
    </div>
  </div>
{{else}}
<p>Não há postagens para exibir</p>
{{/each}}